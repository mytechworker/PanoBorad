import styled from 'styled-components';

export const OnlineBookingWrapper = styled.div`
  .ins-button {
    background-color: ${({ theme }) => theme.colors.white};
    color: ${({ theme }) => theme.colors.primary};
  }
`;
